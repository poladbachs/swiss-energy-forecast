{
  "installCommand": "cd frontend && npm ci",
  "buildCommand": "cd frontend && npm run build && rm -rf ../api/site && cp -R dist ../api/site",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/main.py"
    }
  ]
}
