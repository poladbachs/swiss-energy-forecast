DATABASE_URL=postgresql://postgres:postgres@localhost:5432/energy
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/energy_test
ENTSOE_API_KEY=your-key-here
# DATA_SOURCE=swissgrid  # fallback, no key needed
# DATA_SOURCE=entsoe     # switch to this once ENTSOE_API_KEY is set
DATA_SOURCE=swissgrid
# Use local MLflow for offline work:
# MLFLOW_TRACKING_URI=http://localhost:5001

# Or use DagsHub MLflow with auth handled by dagshub.init():
# MLFLOW_TRACKING_URI=https://dagshub.com/poladbachs/swiss-energy-forecast.mlflow
# DAGSHUB_REPO_OWNER=poladbachs
# DAGSHUB_REPO_NAME=swiss-energy-forecast
